# Composer dependencies (install via `composer install`)
/vendor/
composer.phar
composer.lock

# Node
/node_modules/

# PHPUnit generated files
.phpunit.result.cache
.phpunit.cache/

# Build artifacts
*.zip
/build/

# OS / editor
.DS_Store
.idea/
*.swp
*.swo
*~

# AI session scaffolding — never commit (contains internal hostnames/paths)
docs/superpowers/
